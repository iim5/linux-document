```
$ stat <file name> # Show all infomation of a file (size, permission, Uid, Gid, access/modify/change time)
$ readlink -f <file name> # Show the absolute path of a file
$ locate <file name> # Find all file with name - need to install, not a base command

```
